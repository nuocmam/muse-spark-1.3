# Canonical prompt

Same prompt for every model. Do not rewrite when adding a new run.

```
Create a playable penguin-themed Tetris static web page here. Arrow keys move and rotate, line clears, score, game over. Ice penguin theme, local HTML CSS JS only. No network. Must load by opening index.html.
```
